services:
  _defaults:
    autowire: true
    autoconfigure: true

  # DataFixtures 在测试环境中使用
  Tourze\RSSFeedCollectBundle\DataFixtures\:
    resource: '../../DataFixtures/'
    tags: ['doctrine.fixture.orm']
